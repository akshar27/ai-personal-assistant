fastapi
uvicorn[standard]
python-dotenv
pydantic
itsdangerous

langgraph
langgraph-checkpoint-sqlite
langgraph-checkpoint-postgres
langchain-core
langchain-openai
langsmith
numpy

# Persistence: SQLite locally, Postgres + pgvector in production (DATABASE_URL)
SQLAlchemy>=2.0
alembic
psycopg[binary]
psycopg-pool
pgvector
cryptography

google-auth
google-auth-oauthlib
google-api-python-client

# Optional: local LLM fallback (LLM_PROVIDER=openai_first / ollama).
# The code degrades gracefully if this is not installed.
langchain-ollama
